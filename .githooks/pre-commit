#!/bin/sh
set -e

# Code health gate (only runs when cs CLI and CS_ACCESS_TOKEN are present)
if command -v cs > /dev/null 2>&1 && [ -n "$CS_ACCESS_TOKEN" ]; then
    cs delta --git-hook --staged
fi

mix format --check-formatted
mix hex.audit
mix compile --warnings-as-errors
